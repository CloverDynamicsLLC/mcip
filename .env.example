# ==============================================
# MCIP (Machine Customer Interaction Protocol)
# Environment Configuration Example
# ==============================================

# ----------------------------------------------
# Core Application Settings
# ----------------------------------------------
# Port for the application to listen on
PORT=8080

# OpenAI API Key for vectorization and AI processing
# REQUIRED
OPENAI_API_KEY=sk-your-api-key-here

# ----------------------------------------------
# Infrastructure Connection
# ----------------------------------------------
# Redis Hostname (use 'redis' if running in Docker, 'localhost' if local)
REDIS_HOST=redis

# Qdrant URL (use 'http://qdrant:6333' if running in Docker, 'http://localhost:6333' if local)
QDRANT_URL=http://qdrant:6333

# ----------------------------------------------
# Ingestion Source Configuration
# ----------------------------------------------
# URL to fetch products from (e.g., Vendure Shop API)
SOURCE_URL=https://demo.vendure.io/shop-api

# Strategy to use for parsing the response
# Options: VENDURE, OPENAPI
SOURCE_STRATEGY=VENDURE

# GraphQL Query (Optional)
# If using a GraphQL source, provide the minimized query string here.
# Leave empty if using REST or if the strategy handles it.
GRAPHQL_QUERY=

# Storefront URL for constructing product links
# Example: http://ec2-176-34-75-180.eu-west-1.compute.amazonaws.com:8001
STOREFRONT_URL=http://localhost:8001

# ----------------------------------------------
# Security
# ----------------------------------------------
# Admin API Key to protect sensitive endpoints (like /admin/sync)
# You must provide this key in the 'x-admin-api-key' header when making requests.
ADMIN_API_KEY=secret-admin-key
